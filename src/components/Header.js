import React from "react";
import { Flex } from "../elements";

const Header = props => {
  return (
    <Flex
      styles={{
        height: "100px",
        backgroundColor: "#fff",
        zIndex: 10,
        position: "absolute",
      }}
    >
      asdsadasdsadsa
    </Flex>
  );
};

export default Header;
